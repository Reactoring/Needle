{
  "kind": "collectionType",
  "collectionName": "channel_listings",
  "info": {
    "singularName": "channel-listing",
    "pluralName": "channel-listings",
    "displayName": "Channel Listing",
    "description": "Annonce publiee sur un canal marketplace pour une unite vendable"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "tenant": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::tenant.tenant"
    },
    "sellableUnit": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::sellable-unit.sellable-unit",
      "inversedBy": "channelListings"
    },
    "channel": {
      "type": "enumeration",
      "enum": ["discogs"],
      "default": "discogs",
      "required": true
    },
    "externalListingId": {
      "type": "string"
    },
    "externalUrl": {
      "type": "string"
    },
    "status": {
      "type": "enumeration",
      "enum": ["not_published", "pending", "published", "failed", "removed", "sync_error"],
      "default": "not_published",
      "required": true
    },
    "publishedPrice": {
      "type": "decimal",
      "min": 0
    },
    "lastSyncedAt": {
      "type": "datetime"
    },
    "lastError": {
      "type": "text"
    }
  }
}
